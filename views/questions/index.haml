%h1= t(:faq)

- if @questions.empty?
  %h2= t(:no_questions)

  %a{href: '/questions/new'}= t(:'questions.ask')

- @questions.each do |q|
  .row.question
    .span6
      %h3
        %a{ href: "/preguntas/#{q.permalink}"}= q.question
      %p= q.answer
    .span1.small.actions
      %a{ href: "/questions/#{q.permalink}/edit", class: 'btn'}= t(:edit)
      %form{ action: "/questions/#{q.permalink}", method: 'post'}
        %input{ type: 'hidden', name: '_method', value: 'delete'}
        %input{ type: 'submit', value: t(:delete) , class: 'btn btn-danger'}
